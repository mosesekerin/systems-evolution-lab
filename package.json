{
  "name": "notes-service",
  "version": "2.0.0",
  "description": "Minimal REST + UI notes service — Express, EJS, local JSON storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  }
}
